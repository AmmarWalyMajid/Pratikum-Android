<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pratikum.pratikummp">

    <application
        android:name=".Data.db.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.noActionBar">
        <activity android:name=".ui.ProfilActivity"></activity>
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.ViewRoomDataActivity" />
        <activity android:name=".ui.RoomDataActivity" />
        <activity android:name=".ui.AddRoomDataActivity" />
        <activity android:name=".ui.SharedPrefActivity" />
        <activity android:name=".ui.MainFragment" />
        <activity android:name=".MainGet" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>